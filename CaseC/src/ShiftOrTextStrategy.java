public class ShiftOrTextStrategy implements SortingStrategy{
    @Override

    //Concrete Strategies implement different variations of an algorithm the context uses

    public int getCount() {
        return 0;
    }

    @Override
    public void execute(String text) {

    }
}
